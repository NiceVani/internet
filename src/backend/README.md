# Backend

Separate Directory

`storage` is directory for storing `image` file and `document` pdf file

```bash
easyroom-reservation/
├── booker_backend/
├── admin_backend/
├── executive_backend/
└── storage/
    ├── booking_document/
    └── equipment_img/
```
